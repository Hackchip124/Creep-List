# Core dependencies
streamlit==1.28.1
pandas==2.0.3
plotly==5.17.0

# Database
sqlite3  # Built-in, no need to specify

# Utilities
pathlib  # Built-in
datetime  # Built-in
hashlib  # Built-in
json  # Built-in
random  # Built-in
string  # Built-in
os  # Built-in
shutil  # Built-in
zipfile  # Built-in
io  # Built-in
time  # Built-in